.Wrapper
  .Sidebar
    .User-name
      .Top-items
        .Top-items__name
          = current_user.name
        .Top-items__icons
          = link_to new_group_path do
            = icon('far', 'edit',class: "icons__edit")
          = link_to edit_user_path(current_user) do
            = icon('fas', 'cog')
    .Group-list
      - current_user.groups.each do |group|
        .Group
          = link_to '#' do
            .Group__name
              = group.name
            .Group__message
              メッセージはまだありません